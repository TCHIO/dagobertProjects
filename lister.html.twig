<html>
<body>
<h1>Liste des Bonobos</h1>

<table>
{% for bono in Bonobos %}
        <tr>
                <td>{{ bono.id}}</td>
                <td>{{ bono.age }}</td>
                <td>{{ bono.race}}</td>
                <td>{{ bono.Nourriture}}</td>
                <td>{{ bono.famille}}</td>
                <td><a href="{{ path('myapp_bonobo_modifier', { 'id': boni.id }) }}">Modifier</a></td>
                <td><a href="{{ path('myapp_bonobo_supprimer', { 'id': bono.id }) }}">Supprimer</a></td>
        </tr>
{% else %}
        <tr><td>Aucun bono n'a été trouvé.</td></tr>
{% endfor %}
</table>

<p><a href="{{ path('myapp_bonobo_ajouter') }}">Ajouter un bonobo</a><p>

</body>
</html>
